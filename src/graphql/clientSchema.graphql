type ActiveUser {
    id: ID!
    email: String
    fb_id: String
    fb_picture_url: String
    first_name: String
    last_name: String
    full_name: String
    user_name: String
}

extend type query_root {
    activeUser: ActiveUser
}
