import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
  tileImg: {
    height: "inherit",
    width: "auto",
  },
  nameContainer: {
    display: "flex",
  },
  editCard: {
    height: "inherit",
  },
}));
